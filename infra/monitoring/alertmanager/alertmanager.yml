global:
  resolve_timeout: 5m
route:
  receiver: 'telegram'
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 2m
receivers:
  - name: 'telegram'
    webhook_configs:
      - url: 'http://127.0.0.1:5001/'
        send_resolved: true
