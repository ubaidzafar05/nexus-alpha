{
  "annotations": {"list": []},
  "panels": [
    {
      "type": "graph",
      "title": "Ollama Request Latency (p95)",
      "targets": [{"expr": "histogram_quantile(0.95, sum(rate(nexus_ollama_request_latency_seconds_bucket[5m])) by (le))", "legendFormat": "p95"}]
    },
    {
      "type": "graph",
      "title": "Ollama Failures / Retries",
      "targets": [
        {"expr": "rate(nexus_ollama_failures_total[5m])", "legendFormat": "failures"},
        {"expr": "rate(nexus_ollama_retries_total[5m])", "legendFormat": "retries"}
      ]
    },
    {
      "type": "graph",
      "title": "Retrain Attempts / Accepted / Rejected",
      "targets": [
        {"expr": "rate(nexus_retrain_attempts_total[1h])", "legendFormat": "attempts"},
        {"expr": "rate(nexus_retrain_accepted_total[1h])", "legendFormat": "accepted"},
        {"expr": "rate(nexus_retrain_rejected_total[1h])", "legendFormat": "rejected"}
      ]
    }
  ],
  "title": "Nexus-Alpha LLM & Retrain Overview",
  "schemaVersion": 16
}
