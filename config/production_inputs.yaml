project:
  md_source: "/Users/azt/Downloads/NEXUS_ALPHA_FREE_STACK.md"
  environment: "production"
  timezone: "UTC"

exchanges:
  enabled:
    - "binance"
    - "bybit"
  live_capital_enabled: false

infrastructure:
  kafka_bootstrap_servers: "localhost:9092"
  timescaledb_url_secret_ref: "env://TIMESCALEDB_URL"
  redis_url: "redis://localhost:6379/0"
  kubernetes_namespace: "nexus-alpha"
  prometheus_url: "http://localhost:9090"
  grafana_url: "http://localhost:3000"

credentials:
  binance_api_key_set: true
  binance_api_secret_set: true
  bybit_api_key_set: false
  bybit_api_secret_set: false
  kraken_api_key_set: false
  kraken_api_secret_set: false
  anthropic_api_key_set: false
  openai_api_key_set: false
  cryptopanic_api_token_set: true
  vault_token_set: false

policy:
  max_single_position_pct: 0.20
  max_portfolio_drawdown_pct: 0.15
  max_daily_loss_pct: 0.05
  approved_by: "local-operator"
  approval_date: "2026-04-02"

slo:
  tick_latency_p99_ms: 10
  feature_staleness_seconds: 300
  quality_failures_per_hour: 10
  end_to_end_latency_ms: 200

rollout:
  stage_owners:
    paper: "local-operator"
    micro_live: "local-operator"
    small_live: "local-operator"
    production: "local-operator"
  human_signoff_required: true
